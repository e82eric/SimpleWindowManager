int list_processes_run_no_sort(CHAR** linesToFill);
int list_processes_run_sorted_by_cpu(CHAR **linesToFill);
int list_processes_run_sorted_by_private_bytes(CHAR **linesToFill);
int list_processes_run_sorted_by_working_set(CHAR **linesToFill);
int list_processes_run_sorted_by_pid(CHAR **linesToFill);
